<%= form_for(@micropost) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :content, placeholder: "撰写新信息..." %>
  </div>
  <%= f.submit "上传", class: "btn btn-large btn-primary" %>
<% end %>